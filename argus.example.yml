# Argus Security Scanner Configuration
# Copy to argus.yml and customize for your project
version: "1.0"

scanners:
  bandit:
    enabled: true
    path: "."
    # config_file: "pyproject.toml"  # Optional Bandit config

  trivy-iac:
    enabled: true
    path: "infrastructure"
    severity_threshold: medium

  clamav:
    enabled: true
    path: "."

  gitleaks:
    enabled: true

  osv:
    enabled: true

  checkov:
    enabled: true
    path: "infrastructure"

  opengrep:
    enabled: true
    path: "src"

  supply-chain:
    enabled: true

  # container:
  #   enabled: false
  #   image_ref: "myapp:latest"
  #   scanners: "trivy,grype,syft"

  # zap:
  #   enabled: false
  #   target_url: "http://localhost:3000"

reporting:
  formats:
    - terminal
    - sarif
  severity_threshold: high
  output_dir: "./argus-results"

# Execution backend configuration
execution:
  # auto: try local tools first, fall back to Docker containers
  # local: only use locally installed tools
  # docker: always use containers
  backend: auto

  # Override container registry for private/air-gapped environments
  # registry: "registry.internal.corp/argus"

  # Container pull policy
  # always: pull latest on every run
  # if-not-present: pull only if image not cached locally
  # never: fail if image not cached (for air-gapped)
  pull_policy: if-not-present
