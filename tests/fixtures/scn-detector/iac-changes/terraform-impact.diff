diff --git a/terraform/storage.tf b/terraform/storage.tf
index xyz9876..abc1234 100644
--- a/terraform/storage.tf
+++ b/terraform/storage.tf
@@ -5,12 +5,8 @@ resource "aws_s3_bucket" "data" {
   bucket = "company-sensitive-data"

-  server_side_encryption_configuration {
-    rule {
-      apply_server_side_encryption_by_default {
-        sse_algorithm = "AES256"
-      }
-    }
-  }
+  # Encryption disabled for migration
+  # TODO: Re-enable after data migration complete

   tags = {
     Name = "sensitive-data"
@@ -22,7 +18,7 @@ resource "aws_security_group" "app" {
   ingress {
     from_port   = 443
     to_port     = 443
     protocol    = "tcp"
-    cidr_blocks = ["10.0.0.0/8"]
+    cidr_blocks = ["0.0.0.0/0"]  # Open to public for testing
   }
 }
